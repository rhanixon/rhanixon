import * as types from '../constants/ActionTypes';

export default (state = [], action = {}) => {
    switch (action.type) {
      case types.PURCHASE:
        return [
            ...state,
            action.data
          ];
  
      default:
        return state;
    }
  }